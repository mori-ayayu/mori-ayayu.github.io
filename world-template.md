# 世界资料 · TreeView 模板

「世界」页面（菜单 → 「世界」）用树视图展示世界设定。
当前内容为《蜩坂町町势要览（1999）》的完整结构化版本，数据在 **`world-data.js`**（由 index.html 在 stage.js 之前加载）。

## 节点结构

```js
{
  id:   '唯一标识',                // 英文/拼音，方便以后引用
  label:'显示名',                  // 漢字优先
  ruby: 'ふりがな',                // 振假名注音（可省略）
  lines: ['展开后显示的详情行'],    // 人物信息 / 说明文；没有可省略
  open:  false,                    // 是否默认展开（可省略）
  children: [ 子节点… ]            // 没有子项可省略
}
```

## 层级模板（以房屋为例）

```
家屋
└─ ○○家                ← lines: 一句话描述
   ├─ 一階
   │  ├─ 居間
   │  │  ├─ 箪笥               ← lines: 家具说明
   │  │  │  ├─ 一段目の抽斗
   │  │  │  │  └─ 古写真 など    ← lines: 里面的东西
   │  │  │  ├─ 二段目の抽斗
   │  │  │  │  └─ 底の隠し（夹层）
   │  │  │  │     └─ 封筒 など
   │  │  │  └─ 三段目の抽斗
   │  │  ├─ ラジオ
   │  │  └─ 隠し棚
   │  └─ 台所
   ├─ 二階
   ├─ 庭
   └─ 地下室
```

（上图仅为层级关系示意；界面内不显示 ├ │ └ 这类连线，层级只用缩进表达。）

人物节点用 `lines` 写详细资料：

```js
{
  id: 'p-xxx', label: '榊原 千歳', ruby: 'さかきばら ちとせ',
  lines: ['性別：—', '年齢：—', '住処：—', '職業：—', '備考：—']
}
```

## 填写规则

- 每层都可以继续展开：家屋 → 一階 → 居間 → 箪笥 → 抽斗 → 内物 →（夹层）→ …
- 可开合/可拆的家具（抽屉、柜门、暗格）直接当 `children` 继续嵌套；一层抽屉就是一层节点
- **没有内容的项暂时空着**：`lines` 写 `['（未設定）']`、或干脆省略 —— 界面上显示空白，以后随时补
- 有漢字的名称建议都写 `ruby` 注音（界面会自动以小字标在汉字上方）
- `lines` 里的长文本会自动按树区宽度**折行**显示，不用手动断行
- 展开状态不持久化：刷新后回到数据里标了 `open: true` 的节点（通常只有根节点）
- 提示：需要跨行展示的独立段落，直接写成 `lines` 数组的多个元素即可（元素间自动换行）

## 操作方式

- 点击行：展开 / 折叠（▼ 展开 · ▶ 折叠）
- 鼠标滚轮 / 手机手指上下滑动：滚动
- 点空白处：返回菜单
